DATABASE_URL=

# 같은 공고/개체(구조동물/포인핸드/발견제보) 안에서 이 값 이상 코사인 유사도가 나오는 사진은
# 같은 임베딩 공간 내에서 중복 저장하지 않는다 (기본 0.97).
DEDUP_SIMILARITY_THRESHOLD=0.97

EMBEDDING_MODEL_VERSION_KEY=

DOG_EMBEDDING_CHECKPOINT=
CAT_EMBEDDING_CHECKPOINT=

# scripts/collect_dataset.py, scripts/collect_shelter_balanced.py 전용
# (data.go.kr 유기동물 공공데이터포털 OpenAPI 일반 인증키/엔드포인트)
SERVICE_KEY=
BASE_URL=